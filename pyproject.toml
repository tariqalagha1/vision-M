[project]
name = "vision-m"
version = "1.0.0"
description = "Integrated intelligence platform: Atlas orchestration + H-Scraper execution"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
